extends GPUParticles2D

@export var one_shot_flag := true



func _ready() -> void :
	one_shot = one_shot_flag
	emitting = true


func _physics_process(_delta: float) -> void :
	if not emitting:
		queue_free()
